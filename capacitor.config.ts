import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chicken.tander',
  appName: 'ChickenTander',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
